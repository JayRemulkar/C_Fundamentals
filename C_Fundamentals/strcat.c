#include<stdio.h>
#include<string.h>

int main()
{
    char arr[] = "jay";
    char brr[] = "remulkar";
    strcat(arr, brr);       //  strcat() is used to concate two strings.

    printf("%s", arr);
    //printf("%s",brr);

    
    //printf("%s",crr);

    return 0; 

}